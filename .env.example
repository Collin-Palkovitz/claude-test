# Copy to .env (git-ignored) and fill in. Never commit real keys.

# Alpaca — create keys at https://app.alpaca.markets (paper keys first!)
ALPACA_API_KEY=
ALPACA_API_SECRET=

# Twilio — for SMS trade notifications (Phase 3)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=
NOTIFY_TO_NUMBER=
